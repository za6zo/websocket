# ============================================
# Za6Zo WebSocket Services Configuration
# Copy this file to .env and fill in your values
# ============================================

# Environment
NODE_ENV=production

# WebSocket Server Configuration
WS_PORT=8080
HTTP_PORT=8090

# API Configuration
# Your Next.js API URL (where your main backend is hosted)
API_URL=https://your-api-domain.com

# WebSocket URL (for bridge connection)
# In production, this should be the public WebSocket URL
# For Dokploy, this will be ws://websocket:8080 (internal network)
WEBSOCKET_URL=ws://localhost:8080

# Security
# JWT Secret (MUST match your API server)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Push Notifications (Optional)
# Expo Access Token for push notifications
EXPO_ACCESS_TOKEN=

# Firebase Cloud Messaging (Optional)
FCM_SERVER_KEY=

# ============================================
# Deployment Notes:
# ============================================
# 1. For Dokploy deployment:
#    - Set API_URL to your deployed Next.js API domain
#    - Set WEBSOCKET_URL to your deployed WebSocket domain
#    - Ensure JWT_SECRET matches your API server
#
# 2. For local development:
#    - Use localhost URLs
#    - Can use ngrok for testing with mobile apps
#
# 3. Security:
#    - NEVER commit .env file to git
#    - Use strong, random JWT_SECRET in production
#    - Keep EXPO_ACCESS_TOKEN and FCM_SERVER_KEY secret
# ============================================
